#!/usr/bin/env bash
set -euo pipefail

pane="${CRANK_MUX_TARGET:-}"
if [[ -z "$pane" ]]; then
  pane="${TMUX_PANE:-}"
fi
if [[ -z "$pane" ]]; then
  exit 0
fi

crank nudge --pane "$pane" >/dev/null 2>&1 || true
